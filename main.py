from DescriptiveStatistics import DescriptiveStatistics as ds

# Enter data here
l = []

# Measure of center
ds.mean(l)
ds.Median(l)
ds.mode(l)

# Measure of spread
ds.five_number_summary(l)
ds.variance_p(l)
ds.standard_deviation_p(l)